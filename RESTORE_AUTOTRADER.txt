The last two autotrader.py commits on main are stubs. Restore the last good file from git history:

    git fetch origin
    git checkout c885db47 -- backend/src/autotrader.py

Then restart the trading engine.

Do not git pull again until autotrader.py on GitHub is the full ~800 line file again (not a RuntimeError stub).
